// One export per block family. Each block gets ONE schema type with a
// `variant` field for its different looks — not a separate type per variant.
// See README.md for the full convention and a worked example.
//
// import { heroBlock } from './heroBlock';
// export const blocks = [heroBlock];

export const blocks = [];
