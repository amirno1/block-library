// One export per block family — the React component that renders it,
// switching on the block's `variant` field internally. See README.md.
//
// export { default as HeroBlock } from './HeroBlock';

export {};
